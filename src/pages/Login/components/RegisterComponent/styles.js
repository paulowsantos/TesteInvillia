import styled from 'styled-components';
import { height } from '../../../Onboard/styles';

export const Container = styled.View`
  width: 100%;
  height: ${height * 0.7};
  justify-content: space-between;
`;
